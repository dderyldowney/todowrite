id: TSK-UPDATE-SCHEMA-FILE
layer: Task
title: Update todowrite.schema.json with status fields
description: >
  Add optional status tracking fields to the JSON schema including status enum,
  progress integer (0-100), started_date and completion_date as ISO 8601 strings,
  and assignee as string. Ensure all fields are optional and include proper
  validation rules and descriptions.
metadata:
  owner: schema-engineer
  labels: ["work:validation", json-schema, optional-fields, status-tracking]
  severity: high
  work_type: validation
links:
  parents: [STP-EXTEND-SCHEMA]
  children: [SUB-ADD-STATUS-ENUM, SUB-ADD-PROGRESS-FIELD, SUB-ADD-DATE-FIELDS]
